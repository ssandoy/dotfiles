#!/bin/sh
set -eu

# explorer.exe opens the URL in the Windows default browser but reliably
# exits 1 even on success, which makes callers like `gh --web` report a
# false failure. Normalize the exit code here.
explorer.exe "$@" >/dev/null 2>&1 || true
