# Copy to ~/.config/acli/env.local and fill in local-only values.
# Keep env.local out of git.
#
# acli jira auth login expects an Atlassian account API token without scopes.

export ATLASSIAN_EMAIL="you@example.com"
export ATLASSIAN_API_TOKEN="your-api-token"
