interface:
  display_name: "Suggest Commit"
  short_description: "Draft and copy commit summaries"
  default_prompt: "Use $suggest-commit to review this conversation and repository diff, highlight the important changes, suggest a Conventional Commit message, copy it to the clipboard, and commit it if requested."
